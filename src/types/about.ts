export interface TeamMember {
  name: string;
  role: string;
  spec: string;
}

export interface CompanyValue {
  icon: string;
  title: string;
  body: string;
}

export interface Milestone {
  year: string;
  title: string;
  body: string;
}
