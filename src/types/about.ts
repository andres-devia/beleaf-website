export interface TeamMember {
  key: string;
  name: string;
  role: string;
  spec: string;
}

export interface CompanyValue {
  key: string;
  icon: string;
  title: string;
  body: string;
}

export interface Milestone {
  year: string;
  title: string;
  body: string;
}
