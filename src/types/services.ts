export interface ServiceSummary {
  icon: string;
  title: string;
  body: string;
  slug: string;
  live: boolean;
}

export interface SeoPillarCard {
  k: string;
  title: string;
  body: string;
}

export interface StackTool {
  name: string;
  icon: string;
  category: string;
}

export interface ProcessStep {
  n: string;
  title: string;
  body: string;
}
